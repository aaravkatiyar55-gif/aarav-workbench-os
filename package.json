{
  "name": "aarav-study-desk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A lightweight, distraction-free study desk to pick one next action, take quick notes, and leave session handoffs.",
  "scripts": {
    "check": "node --check script.js && node --check projects.js"
  }
}
